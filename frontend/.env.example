# Copy to .env for local dev. For production (Cloudflare Pages), set
# VITE_API_BASE as a build environment variable in the Pages dashboard
# instead, pointing at your deployed Render backend URL.
VITE_API_BASE=http://localhost:8000
